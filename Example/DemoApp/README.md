# Demo App

The full demo app is intentionally deferred by the Phase 2 decision gate.

Phase 3 must first reconstruct a filtered hierarchy from a real app's live
rendered graph. Building overlay and inspector demonstrations before that would
hide the central technical risk.

The current executable proof-of-concept is:

```bash
swift run SwiftUIInspectorResearch
```
